{
  "tool": "codex",
  "server": "example_stdio",
  "output_path": ".codex/config.toml",
  "format_type": "toml",
  "mapping": {
    "name": "{{name}}",
    "transport": "{{transport}}",
    "url": "{{url}}",
    "command": "{{command}}"
  }
}
